function delta_deg = shortestAzimuthDeltaDeg(from_deg,to_deg)
%SHORTESTAZIMUTHDELTADEG Public facade for seam-aware angular difference.
%   The implementation lives in AZEL.GEOMETRY.SHORTESTAZIMUTHDELTADEG.

    delta_deg = azel.geometry.shortestAzimuthDeltaDeg(from_deg,to_deg);
end
